# M1 returned evidence

The user places untouched Mac return archives in `incoming/`. That directory is
ignored so generated archives are never staged accidentally. After validation,
small normalized manifests and factual analyses belong in versioned sibling
paths; original returns remain immutable.

Current expected inventory return name:

```text
cuda4as-m1-mac-inventory-return-<UTC>.tgz
```
